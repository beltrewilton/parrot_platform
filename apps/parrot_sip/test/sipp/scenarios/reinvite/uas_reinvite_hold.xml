<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  UAS scenario: Receive INVITE and re-INVITE (hold) from ParrotSip UAC

  Flow:
  1. Receive INVITE from ParrotSip
  2. Send 200 OK
  3. Receive ACK
  4. Receive re-INVITE (hold - sendonly SDP)
  5. Send 200 OK
  6. Receive ACK
  7. Receive BYE
  8. Send 200 OK
-->

<scenario name="UAS re-INVITE Hold">
  <!-- Receive initial INVITE -->
  <recv request="INVITE" crlf="true"/>

  <!-- Send 200 OK for initial INVITE -->
  <send>
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:];tag=[pid]SIPpTag01[call_number]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=- 3600 3600 IN IP4 [local_ip]
      s=SIPp
      c=IN IP4 [local_ip]
      t=0 0
      m=audio 6000 RTP/AVP 0 8 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=sendrecv

    ]]>
  </send>

  <!-- Receive ACK for initial INVITE -->
  <recv request="ACK" crlf="true"/>

  <!-- Receive re-INVITE (hold) -->
  <recv request="INVITE" crlf="true"/>

  <!-- Send 200 OK for re-INVITE -->
  <send>
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=- 3601 3601 IN IP4 [local_ip]
      s=SIPp
      c=IN IP4 [local_ip]
      t=0 0
      m=audio 6000 RTP/AVP 0 8 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=recvonly

    ]]>
  </send>

  <!-- Receive ACK for re-INVITE -->
  <recv request="ACK" crlf="true"/>

  <!-- Receive BYE -->
  <recv request="BYE" crlf="true"/>

  <!-- Send 200 OK for BYE -->
  <send>
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Content-Length: 0

    ]]>
  </send>

</scenario>
