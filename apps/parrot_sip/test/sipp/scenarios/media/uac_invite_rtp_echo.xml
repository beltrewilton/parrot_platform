<?xml version="1.0" encoding="UTF-8" ?>
<!--
  UAC scenario that sends INVITE with SDP and uses -rtp_echo to echo
  back any RTP packets received from the UAS.

  This tests:
  - SIPp sends INVITE with SDP offer
  - Parrot UAS receives INVITE, negotiates SDP, starts sending RTP
  - SIPp echoes received RTP back to Parrot
  - Parrot receives echoed RTP packets

  Usage:
    sipp -sf uac_invite_rtp_echo.xml -rtp_echo <target>:<port>
-->
<scenario name="UAC with RTP Echo">
  <!-- Send INVITE with SDP -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:test@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag01[call_number]
      To: test <sip:test@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Subject: Test Call
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[local_ip_type] [local_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Receive 100 Trying (optional) -->
  <recv response="100" optional="true" />

  <!-- Receive 180 Ringing (optional) -->
  <recv response="180" optional="true" />

  <!-- Receive 200 OK with SDP -->
  <recv response="200" rtd="true" />

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:test@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag01[call_number]
      To: test <sip:test@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Wait for media to flow (2 seconds) -->
  <!-- SIPp will echo RTP during this time if -rtp_echo is enabled -->
  <pause milliseconds="2000"/>

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[
      BYE sip:test@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag01[call_number]
      To: test <sip:test@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Receive 200 OK for BYE -->
  <recv response="200" />
</scenario>
